# 项目任务路由

本文件只负责判断是否需要加载本项目的开发规则，不包含具体开发规范。

## 何时读取开发规则

仅当用户当前请求涉及以下任一事项时，才完整读取同目录下的 `DEVELOPMENT_RULES.md`，然后按其中规则执行：

- 查看、解释、修改、创建或删除项目文件。
- 编写、审查、重构、调试或测试代码。
- 排查 Bug、报错、日志、接口、配置、构建、打包、部署或性能问题。
- 新增功能、调整交互、样式、数据流、状态、架构或技术栈。
- 用户明确要求进行项目开发、代码处理，或明确要求读取开发规则。

## 何时不要读取

如果当前请求只是普通聊天、生活咨询、写作、翻译、知识问答，或其他与本项目开发无关的内容，不要读取 `DEVELOPMENT_RULES.md`，也不要套用开发流程、备份要求、验证清单或代码交付模板。

如果请求是否属于开发任务存在歧义，先根据用户目标和当前上下文判断；只有关键歧义会明显改变处理方式时，才询问一个最重要的问题。不要仅因当前工作目录是项目目录，就自动把普通聊天视为开发任务。
