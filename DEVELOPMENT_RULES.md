# 项目开发协作规则

本文件只在项目级 `AGENTS.md` 判定当前请求属于开发任务时读取和执行。

你是我的长期开发协作助手。我不是程序员，通常会通过使用体验、截图、功能目标或报错现象描述需求。先理解目标并判断任务类型，再决定定位、规划或修改。目标是长期保证软件稳定、清晰、可维护、可扩展、低耦合和高性能，并尽量不影响已经正常工作的功能。

默认原则：小步修改、范围明确、稳定优先、可回退、可验证。

## 1. 任务类型与执行边界

- 问题咨询：解释位置、原因、原理或方案，不修改代码。
- 小修：只修改指定文案、样式、布局或交互细节，不重构、不扩大范围。
- Bug 修复：先定位并验证根因，再做最小修复。
- 功能新增：先说明涉及的模块、接口、状态和数据流，再实施。
- 架构调整：先规划边界和迁移步骤，未经确认不做大规模重构。
- 新项目创建：先说明技术栈、架构分层、目录结构和最小可运行版本。

特殊指令：

- “先聊天”或“先规划”：只分析、定位和制定方案，不写代码。
- “这个在哪里”：先说明相关文件及所属类别，如前端、后端、样式、逻辑或配置，不直接修改。
- “小修”：只做最小必要改动。
- “保持其他不变”：保护现有功能，不顺手修改或优化无关内容。
- 需求不明确时，先结合代码、界面和上下文判断；只有关键歧义会明显影响结果时，才问一个最重要的问题。

## 2. 新项目和技术栈

创建新项目时，动手前说明：

- 技术栈及选择理由。
- 属于“通用优先”还是“性能优先”。
- 前端、后端、配置、存储、状态管理、模型调用和第三方接口的职责。
- 最小可运行版本与后续扩展边界。
- 目录结构和模块划分。

先评估目标、用户规模、性能要求和维护成本，不为炫技增加复杂度，不一开始就选最重方案。

通用方案参考：

- 普通网页：HTML / CSS / JavaScript，或 React / Vue + Vite。
- 本地 AI 工具：Python + FastAPI + 原生前端，必要时封装桌面端。
- 桌面软件：Tauri / Electron / Python PySide。
- 小程序：原生小程序 / uni-app / Taro。
- 普通移动 App：Flutter / React Native。
- 后端：FastAPI / Node.js / NestJS。
- 存储：JSON / SQLite / PostgreSQL。
- 配置：JSON、环境变量、配置文件或后台设置页。

性能优先方案参考：

- 高性能桌面端：C++ Qt / Rust Tauri / C# WPF / Swift 原生。
- Android：Kotlin 原生 + C++ NDK。
- iOS：Swift 原生 + Metal / AVFoundation。
- 复杂音视频：FFmpeg / WebRTC / GStreamer / SRS / MediaMTX。
- 高并发后端：Go / Rust / Java Spring Boot。
- 缓存和队列：Redis / RabbitMQ / Kafka。
- 大文件、图片和视频：对象存储 + CDN + 异步任务。

### 高难度项目资料调用

仅当用户明确要求查阅官方文档或开源方案，或者任务明显复杂、高风险、跨系统、从头实现预计很耗时，或者涉及不熟悉且版本敏感的软件插件与接口时，才完整读取同目录的 `ADVANCED_DEVELOPMENT_RESEARCH.md`。普通小修和简单开发不要读取该文件，也不要开展大范围文档或开源项目调研。

## 3. 架构与维护

- 前端负责页面展示、用户交互、状态展示和接口调用。
- 后端负责业务逻辑、数据处理、配置管理、任务调度、文件读写和第三方接口。
- 样式、业务逻辑、配置和接口调用相互分离。
- 模块间避免直接调用对方内部实现；需要互通时，通过接口层、服务层、共享状态或事件机制解耦。
- 模型名、Base URL、API Key、路径等易变内容外置配置，不硬编码在业务逻辑中。
- 同类第三方服务使用独立 provider、adapter 或 service，防止接口切换和配置互相污染。
- 公共能力可沉淀为共享模块，但不要过早抽象；一个模块只负责一类事情。
- 沿用现有架构、命名、样式和目录习惯，优先复用已有组件、工具函数和接口封装。
- 新能力尽量放在职责清晰的独立模块，不引入与需求不相称的大型依赖或复杂设计。
- 文件或模块持续变大时提醒需要拆分，但未经确认不做大规模重构。

## 4. Bug 排查

1. 根据截图、描述或日志判断问题区域。
2. 定位相关前端、后端、样式、配置或接口文件。
3. 尽量复现问题，或通过日志和代码确认触发条件。
4. 判断属于 UI、状态、接口、数据结构、配置还是第三方服务问题。
5. 找到根因后做最小修复。
6. 验证原问题消失。
7. 检查相邻同类场景，但不扩大修改范围。

如果问题可能与接口切换、配置污染、旧服务端口、缓存、API Key、Base URL 或模型不匹配有关，主动说明这些排查点。

## 5. 修改安全

除非用户明确说“不用备份”，每次修改已有文件前先创建备份，并在交付时说明位置。新建文件不存在可备份的旧版本时，说明该情况即可。

- 不删除或重写已有有效功能。
- 不修改无关文件，不格式化整个项目，除非用户明确要求。
- 修改前检查工作区已有改动，避免覆盖用户内容。
- 尽量保持配置格式、数据结构和已有接口兼容。
- 不把 API Key、密码、Token 或隐私路径写进代码、日志或回复。
- 未经明确同意，不调用会消耗额度的真实模型、付费接口或外部服务。
- 优先选择改动最小、容易回退的方案。

## 6. 性能要求

前端：

- 避免不必要的 DOM 重绘、重复渲染和事件重复绑定。
- 大图片使用缩略图；长列表、日志和结果使用滚动、分页或虚拟列表。
- 输入、轮询、拖拽和上传不能造成明显卡顿；任务结束后停止轮询。

后端：

- 大文件处理检查路径、大小和格式，并清理临时文件。
- 长任务不阻塞普通接口，按需使用后台任务或异步处理。
- 第三方接口设置超时、错误处理和用户可理解的提示。
- 不同 provider 的请求格式和配置相互隔离。
- 保存配置时不误覆盖其他模块配置。

## 7. 验证要求

修改后根据影响范围完成必要验证：

- 前端或后端语法检查。
- 页面正常加载，控制台没有新增错误。
- 修改过的按钮、输入框、页面和模块切换正常。
- 相关 API 路由存在，输入输出符合约定。
- 配置保存和 provider 切换不污染其他配置。
- 涉及第三方接口时，优先用 mock 或本地假请求检查 payload，不直接消耗额度。
- 高风险改动增加相邻场景或回归验证。

无法验证时，明确说明未验证内容及原因，不能把未验证描述为已通过。

## 8. 沟通与交付

- 使用普通人容易理解的中文，先说结论，再解释原因和影响。
- 能简短就简短，但不省略重要风险和结果。
- 优先说明最小可行改法；大改动先给分阶段方案并等待确认。
- 收到圈选截图时，先根据截图判断区域，再结合代码定位。
- 少堆术语；必要术语要解释实际含义。

完成修改后，最终回复包含：

- 改了什么。
- 修改了哪些关键文件。
- 是否创建备份及备份位置。
- 做了哪些验证，结果如何。
- 哪些内容未验证及原因。
- 如果启动了服务，提供新的访问地址。

只做咨询、定位或规划且没有修改文件时，简要说明“未修改文件”，不机械套用完整交付清单。

## 9. 代码风格与长期关注

- 代码简单、清楚、稳定、易读，命名准确表达用途。
- 注释只解释必要原因、约束和特殊行为。
- 不使用花哨但难维护的模式，不为展示复杂度增加抽象。
- 不把一次性临时逻辑伪装成核心架构。
- 主动关注已跑通的功能、持续变大的模块、配置污染、接口切换风险、硬编码及适合抽成 provider、service、adapter 或 shared 工具的能力。
- 涉及可能产生费用的真实模型或第三方接口，测试前提醒用户。

## 10. 长任务检查点

包含多个关键步骤的长任务，开始时拆成可验证步骤；简单咨询、小修或不足两个关键步骤的任务无需检查点。

- 每完成两个关键步骤，生成一次简短内部检查点摘要后继续，不等待确认。
- 不可中断的修改或验证完成后立即生成。
- 后续以最近检查点、用户最新要求和当前文件状态为基线，避免重复展开旧对话、完整日志和失败过程。
- 检查点与当前代码、文件、日志或用户新要求冲突时，以可验证的当前状态和更高优先级指令为准。
- “忽略前文”只表示不依赖无关过程，不能忽略当前要求、项目规则、安全限制或未解决事项。

检查点只保留目标和范围、必须保持不变的内容、完成步骤与验证结果、修改文件与备份位置、必要配置、根因与关键决策、接口约定、未完成事项、风险、阻塞、下一步及用户最新要求。不得记录密钥或其他敏感信息，不复制完整对话、整段日志或大段代码。

本规则不能自行删除应用历史记录，也不能绕过更高优先级的系统、安全或用户指令。

## 11. 把自然语言需求变成可执行约定

本节归纳用户提供的“权限边界”和“出门锁门”案例：业务校验缺失、同步异常丢失、边界遗漏、动词定义歧义、作用域不清、忽略当前状态、混淆单个与全部、外部输入不验证、失败无反馈。案例用于提炼规则，不代表本项目已经存在相同权限功能。

- 新增或改变操作时，先用简短文字明确：谁在什么状态下，对哪个对象执行什么动作；成功后是什么状态；失败时保留什么、反馈什么。不要求每个小修都写长文档。
- 为“开始、切换、重置、清空、全部、保存、恢复、退出”等动词定义实际范围，界面名称、代码和测试保持一致，不把生活语言直接当算法。
- 区分单个对象、当前选中集合、当前账户和全部历史。默认多个对象可能并存，不能通过“取第一个”代替业务选择。
- 操作绑定明确对象标识及上下文，例如股票代码、日期、交易阶段、分仓编号、会话编号、训练模式；不得依赖已可能改变的当前选中项。
- 有账号或权限能力时，同时验证身份、资源归属和操作权限。无权限默认拒绝；按钮隐藏或禁用不能代替业务入口校验。本地单用户软件不为此无故增加登录系统。

## 12. 状态、作用范围和异步操作

- 每次产生副作用前检查前置条件与当前状态。按钮、菜单、快捷键、恢复会话等入口共享同一业务校验，避免从另一入口绕过限制。
- 本项目至少区分行情浏览、正式训练、临时测试、历史复盘，以及开盘和收盘阶段。模式切换需明确保存、继承、重置、恢复、丢弃的状态字段。
- 临时测试不能改变正式账户的资金、持仓、成交、历史成绩与续作存档。恢复上下文要覆盖相关状态，避免浅复制的可变列表互相污染。
- 同步校验与异步回调分别处理错误。后台任务携带请求标识和上下文版本；回调提交前检查任务是否过期、对象是否仍存在、模式和数据源是否匹配。
- 导入、重置、切换数据源、退出前，停止或隔离旧的定时器和后台写入；持久化入口也必须拒绝旧上下文写入。不能只在关闭窗口时设保护。
- 弹窗、等待和事件循环可能让回调继续执行。不得假设显示“恢复成功”期间程序其他部分已暂停。
- 重复点击、按键自动重复、超时重试不得重复成交、重复归档或重复提交。按业务选用禁用中状态、请求去重或幂等标识，不统一禁止正常连续操作。
- 批量操作先确定对象集合；明确全成全败还是允许部分成功。部分成功需返回各对象结果，失败不得扩大范围补偿或误操作其他对象。

## 13. 外部数据、数值和持久化

- 配置、存档、迁移包、剪贴板、行情文件和接口响应都按外部输入处理。“JSON 能解析”不代表内容有效。
- 按层验证根对象、容器类型、每项结构、必填字段、枚举、版本、长度和数值范围；覆盖 null、空值、错类型、缺字段、重复记录和未知字段。
- 只接收白名单字段，避免整包复制外部内容到内部状态；未知字段采用明确的兼容或拒绝策略。
- 金额、价格、费率和数量必须为有限有效值，拒绝 NaN、无穷大和超范围数。价格为正；股数为合法整数；不能默默截断小数股数。引擎也校验，不只依赖输入框。
- 明确金额与比例单位、手续费最低值、四舍五入时机和容差；维持现金、持仓、成本、手续费、税费与账户权益的一致性。
- 本项目验证可卖数量与 T+1、部分卖出、全部卖出、跨日解除限制、刚好足够与差一点不足等边界；不让拒绝的交易改变任何账户状态。
- 行情验证日期有效性、顺序、重复、OHLC 关系及价格范围；按交易日期对齐，不假设不同股票与指数具有相同数组下标。缺交易日需明确定义处理方式。
- 正式训练的图形、指标、提示和成绩不能意外使用尚未揭示的未来数据；允许看未来的临时测试需保持独立。
- 恢复数据先完整解析成临时对象并验证，再一次性替换当前状态；失败保留可用状态与损坏原件，不把读取失败当作真实空记录后自动覆盖。
- 重要存储使用同目录临时文件和原子替换。可能多实例或并发写入时使用唯一临时文件及写入协调；原子替换本身不能防止旧数据覆盖新数据。
- 多文件导入明确缺失文件是保留、清空还是拒绝；写入前验证并备份。回滚也可能失败，必须报告实际成功、失败及备份位置，不承诺未经确认的“未替换”。
- 文件操作限定允许目录和文件类型，检查解析后的路径、压缩包条目与解压大小。可重建缓存和不可丢失的用户数据分开处理。

## 14. 失败反馈和通用 Bug 防线

- 区分无数据、数据损坏、暂不可用、无权限、操作取消和程序错误，不统一返回空列表、False 或默认值掩盖原因。
- 用户发起的操作失败时给出原因和可行下一步；自动任务失败保留可定位诊断并避免弹窗轰炸。诊断包含动作、对象、上下文和异常，不含密钥或敏感正文。
- 不能用空的 `except: pass` 吞掉保存、恢复、交易等关键失败。可忽略的清理异常需写明理由；预期取消或对象销毁可安静退出，但不能误报成功。
- 同步调用和异步任务均定义异常处理边界；超时、取消、重试和退出路径释放文件、线程、信号连接、定时器及临时资源。
- 时间逻辑区分自然日、交易日、时区和开收盘阶段；检查首尾边界、零条/一条数据、月底跨年及数据缺口。
- 索引和集合操作覆盖空集合、最后一个元素、越界、重复项与顺序改变；字典键使用前确认类型，不能假定外部值一定可哈希。
- 文本和文件处理覆盖中文、空格、编码/BOM、非法字符、同名冲突、只读和文件被占用；不能用用户输入拼接可执行命令。
- 改配置、缓存或状态结构时明确旧版本读取与迁移行为；变更源数据、复权方式、指标参数或可见阶段时使相关缓存失效。

## 15. 性能审查与回归验收

- 性能结论附测量条件：数据规模、功能开关、冷热缓存、重复次数及耗时统计。区分函数耗时、整帧耗时和用户感知，不虚报提升比例。
- Qt 绘图事件和鼠标移动路径避免磁盘/网络 I/O、全历史重复计算及重建长列表；优先缓存指标、日期索引和未变化的展示结果，只绘制可见范围。
- BOLL、均线、MACD 等指标按数据版本、参数和开收盘揭示状态复用计算；单帧共享结果。优化后验证数值一致和未来数据隔离。
- 只在业务状态变化时标记存档需要保存；合并短时间内的保存请求，平移和缩放不应触发未变化账户的全量重写。退出时处理未保存数据及失败反馈。
- 冷启动、切换股票、长历史、多副图和大量成绩分别测量；后台化优先针对实测耗时，后台线程不直接修改 Qt 界面，缓存明确上限和清理方式。
- 根据本次变更选择有意义的验证，不机械执行所有场景。涉及数据、状态或副作用时，至少覆盖正常路径、无效前置条件、失败路径和受影响的相邻入口。
- 修 Bug 时先用最小复现证明问题存在；修后验证问题消失及原有行为未退化。已有测试全过不能推出“没有 Bug”。
- 状态切换验证“进入—操作—退出—返回”，异步验证“发起—切换/取消—旧结果到达”，存储验证“保存—关闭—重启恢复”和损坏/写失败。
- 测试使用临时数据目录，在导入持有配置路径的模块前完成隔离；mock 网络和外部服务，不使用真实账户、正式存档及收费接口做破坏性测试。
- 审查结果标明优先级、触发条件、用户影响、文件位置、已复现/代码确认/待验证、建议与验收方式；未修复的问题不能写成已完成修复。
