# 高难度开发资料与方案复用规则

本文件不是常规开发规则。只有 `DEVELOPMENT_RULES.md` 的“高难度项目资料调用”条件被触发时，才完整读取和执行。

- 为特定软件或平台开发脚本、插件、扩展和第三方集成时，先查阅当前官方开发文档、API 参考、SDK、示例和模板，确认支持的语言、版本、权限、接口及发布方式。
- 优先采用官方推荐的语言、框架、SDK、CLI、组件、模板和资源；官方已有稳定能力时不重复实现。
- 只读取与当前任务直接相关的文档章节和示例，并记录关键版本或兼容范围；避免无目的通读整站文档和大量无关资料。
- 复杂项目在自建核心能力前，调查 GitHub 等代码仓库中成熟、活跃且适配目标的开源项目，优先考虑复用、扩展或分阶段迁移。
- 采用开源方案前检查许可证、维护活跃度、版本发布、安全记录、依赖规模、平台兼容性和后续维护成本；保留必要的许可证与版权声明，不盲目复制来源不明或不兼容的代码。
- 引入会明显改变架构、增加重大依赖或需要搬迁现有功能的方案时，先说明候选方案、采用理由、影响范围、风险、迁移步骤和回退方式，获得确认后再实施。
- 官方最新方案与项目现状冲突时，优先保护已经正常工作的功能，说明版本差异，并采用可验证的分阶段升级或适配方案。
