# 已确认 Bug 修复与全局规则更新

本记录是上一份审查报告的修复结果；原审查报告保留当时证据。

## 修复结果

| 编号 | 处理 | 验证 |
| --- | --- | --- |
| B1 导入会话被旧自动保存覆盖 | 导入前停止定时器并设置写入保护；直接保存和定时保存入口均拦截。预期导入失败后恢复原保存安排 | 真实导入函数配合 Qt 事件循环运行，导入结果仍为 IMPORTED；失败路径不改变原文件 |
| B2 异常配置阻断启动 | 指标逐项检查类型，保留合法项；编码/JSON/根结构损坏可降级读取并备份 | 混合字典、列表、null 和合法指标不崩溃；界面显示备份提示 |
| B3 异常历史成绩容器 | 校验容器，隔离坏记录并保留可用记录；覆盖前保存损坏原件 | null 容器返回空结果；混合记录保留有效成绩；验证原件备份内容 |
| B4 续作结构损坏、半恢复 | 校验恢复结构、阶段和主要账户数值后才进入账户恢复流程；保留旧版空本金默认行为 | 根列表、active=null、非法阶段不导致启动中断或替换原账户；正常存档往返和旧版空本金恢复通过 |
| B5 非法价格交易 | 买卖入口拒绝零、负、非有限价格、非法阶段及小数股数；检查金额溢出 | 负价输入返回拒绝，现金保持 10000、持仓为 0；正常交易、T+1 和卖出回归通过 |

损坏数据的备份在原数据目录，文件名为 `原文件名.invalid-唯一标识.bak`。若备份失败，则本次进程阻止对应文件的常规保存/删除，避免损坏原件被默认数据覆盖；界面给出提示。备份失败后的写入保护已有测试。

## 全局规则

- `C:/Users/Administrator/.codex/DEVELOPMENT_RULES.md`：加入第 11—15 节的通用准则，将股票、分仓、Qt 等项目专属表达改为适用范围明确的通用表述。
- `C:/Users/Administrator/.codex/AGENTS.md`：开发任务读取全局准则，再遵守项目规则；普通聊天不加载开发规则。
- 项目内规则保留，其中的训练模式、未来行情隔离、T+1 等继续约束本软件。未批量改动其他已有项目。

## 文件与备份

- 修改：`stock_simulator/app.py`、`config.py`、`persistence.py`、`engine.py`。
- 新增：`stock_simulator/storage_safety.py`、`tests/test_audit_fixes.py`、本记录。
- 调整原审查脚本 `docs/audits/probe_20260906.py`，使其能读取修复后相同输入的结果；回归断言由正式测试负责。
- 业务文件修改前备份：`backups/20260906-confirmed-bug-fixes/stock_simulator/`。
- 全局规则修改前备份：`backups/20260906-confirmed-bug-fixes/global/`。
- 上述目录还保存测试记录与中间版本；未修改真实配置或账户文件。

## 验证

- 修复前新增用例与原用例共 38 项：出现 7 个断言失败和 6 个错误，证实原问题可检测。
- 最终 `python run_tests.py`：44 项通过，17.513 秒，包括原有 28 项和新增 16 项。
- 修改的 Python 模块通过 `py_compile` 语法检查。
- 原审查输入再次运行：配置正常降级；损坏历史返回空列表；损坏会话返回 False；导入结果保持 IMPORTED；负价格交易不改变账户。
- 测试使用临时目录与离屏 Qt，网络启动任务被隔离；提示日志来自故意损坏的数据。

本次完成源码层 Bug 修复；未重新打包 EXE，未对真实通达信目录或已发行可执行文件做全流程测试。BOLL、列表刷新等性能建议尚未实施，本次不声称性能提升。
