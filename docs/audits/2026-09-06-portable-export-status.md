# 便携版同步导出与清理状态

## 已完成

- 当前源码 21 个模块与性能优化版 PYZ 中的代码对象一致；优化此前已在源码完成，本次不需要反向同步或重写业务文件。
- 使用现有导出流程复制性能优化版 EXE 和完整运行依赖，逐文件 SHA256 校验一致。
- 导出位置：`releases/portable-performance-20260906/大A日K股票模拟训练器-便携性能优化版-20260906/`，同级 ZIP 为 48,145,513 字节。
- 便携版带 `portable_mode.flag`；初始 `portable_data` 仅包含默认设置，不含个人路径、续作或历史成绩。
- ZIP CRC 校验通过。便携副本在临时目录离屏启动，3 秒后进程正常存活，基础行情缓存生成在便携目录，没有在临时 AppData 生成设置。
- 51 项测试通过，19.865 秒。上述启动检查不代表真实桌面全流程验收。
- 源码和受保护用户数据的哈希记录见 `2026-09-06-portable-manifest.json`，最终校验未变化。

## 清理未执行

已检查并列出 9 个清理目标：旧 backups、build、复权计算缓存、维护标记和各源码/测试目录的 Python 缓存，合计 48,359,927 字节（约 46.1 MiB）。清单见 `2026-09-06-cleanup-plan.json`。

批量 PowerShell 删除命令被自动审批拒绝；缩小为仅删除项目根目录 `__pycache__` 的固定路径命令后，仍被拒绝。工具两次仅返回 `blocked by policy`，未给出具体理由。未尝试通过其他工具绕过拒绝。

最终检查确认 9 个目标仍存在；没有删除任何缓存或备份。正在运行的性能优化版未关闭、未替换。

`compare_performance.py` 增加了历史基线不存在时的说明，以便后续清理后仍能提示运行当前基准；其修改前副本在尚未删除的 `backups/20260906-cleanup-compare.py`。
